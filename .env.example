# GitHub personal access token
# Required scopes: public_repo, security_events, read:org
# Without this token, rate limit is 60 requests/hour (very slow)
# With this token, rate limit is 5000 requests/hour
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=your_token_here
